# ===========================================
# Local Genius — Pinned Dependencies
# ===========================================
# Install: pip install -r requirements.txt
# ===========================================

# The Brain — LLM SDK
ollama>=0.4.0

# The Logic — Orchestration
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Memory — Vector database for state persistence
chromadb>=0.5.0

# The Gateway — MQTT for hardware communication
paho-mqtt>=2.0.0

# Config — Environment variable loading
python-dotenv>=1.0.0

# Dev tools
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.5.0

# IDE Integration & Proactive Mode (Phase 3)
fastapi>=0.110.0
uvicorn>=0.29.0
watchdog>=4.0.0
